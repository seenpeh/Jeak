version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=jeak-secret-key-12345
    volumes:
      - ./jeak.db:/app/jeak.db
    restart: always
